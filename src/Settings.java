import java.awt.*;

public class Settings {

    Color snakeColor;
    int gameSpeed;
    String gridSize;
    Color backgroundColor;

    // Default Settings
    public Settings() {
        snakeColor = Color.GREEN;
        gameSpeed = 100;
        gridSize = "MEDIUM";
        backgroundColor = Color.BLACK;
    }
}
